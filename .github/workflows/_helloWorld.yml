name: Hello World!

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string

jobs:
  greeting:
    name: Greet user
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Run a one-line script
        run: echo Hello, '${{ inputs.name }}'!